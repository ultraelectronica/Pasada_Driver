# Start Utils

Utility code & constants specific to the Start / Onboarding flow.

* `start_constants.dart` – central place for layout fractions, animation durations and other "magic numbers" used in the start pages.

Keep this folder lightweight: **no stateful logic** – only simple helpers/constants. 